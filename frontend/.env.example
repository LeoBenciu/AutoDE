# Keep empty when nginx or the Vite development server proxies /api.
# For a separately hosted frontend, set the public backend origin. The /api
# suffix is optional, for example: https://autoimport-api.example.com
VITE_API_URL=
