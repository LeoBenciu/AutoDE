anthropic>=0.69.0
pydantic>=2.7
pypdf>=4.2
# Optional OCR sources — enable when the corresponding infra is configured:
# boto3>=1.34          # AWS Textract (Expense + Analyze)
# pytesseract>=0.3.10  # OSD auto-orientation for rotated scans
# pdf2image>=1.17      # page-at-a-time rendering for tesseract
